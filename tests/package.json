{
  "name": "fahrsync-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project fahrsync-test \"node tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.29.1"
  }
}